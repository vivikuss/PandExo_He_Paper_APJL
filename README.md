# PandExo setup (working)

This setup was verified on:
- Python 3.11
- PandExo + pandeia.engine
- JWST NIRSpec PRISM

## Environment setup

Using pip:

```bash
python -m venv venv_pandexo311
source venv_pandexo311/bin/activate
pip install -r requirements.txt

