Recreate environment (Python 3.11 recommended):

python3.11 -m venv venv_pandexo311
source venv_pandexo311/bin/activate
python -m pip install -r requirements.txt

Refdata:
- pandeia_data is not tracked in git (too large). Point PANDEIA_REF_PATH to your local copy.
